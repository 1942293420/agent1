[pytest]
DJANGO_SETTINGS_MODULE = agent_platform.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers -p no:warnings
markers =
    slow: 慢速测试 (>1s)
    integration: 需要外部服务 (Redis/DB)
    smoke: 冒烟测试
